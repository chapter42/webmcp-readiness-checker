{
  "name": "webmcp-readiness-checker",
  "version": "0.2.0",
  "private": true,
  "description": "Chrome extension that audits any page for WebMCP and AI agent-readiness.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chapter42/webmcp-readiness-checker.git"
  },
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "syntax": "node scripts/check-syntax.mjs",
    "lint": "eslint .",
    "package": "node scripts/package.mjs",
    "verify": "npm run syntax && npm test"
  },
  "devDependencies": {
    "eslint": "^9.14.0"
  }
}
